#include "modcell.h"

Population run_moea(MCproblem *mcp, Population *initial_population){
    //WIP
    //Population *output_population = (Population *)malloc(sizeof(Population));
    //allocate_population(&mcp, output_population, mcp.population_size);
    return *initial_population;
}
